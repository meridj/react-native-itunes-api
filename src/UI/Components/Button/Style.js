"use strict";

/**
 * Npm imports
 */
import { StyleSheet } from "react-native";

const Style = StyleSheet.create({
  button: {
    padding: 10,
    margin: 10,
    borderWidth: 2,
    borderRadius: 2,
    alignItems: "center"
  }
});

export default Style;
